document.addEventListener('DOMContentLoaded', () => {
    console.log('NEETprep Clone loaded!');
    
    // Add JavaScript functionality here
    // Example: Load course details dynamically
    const coursesSection = document.getElementById('courses');
    coursesSection.innerHTML += '<p>Course content loaded dynamically.</p>';
});
